CREATE TABLE `Size` (
  `id` <type>,
  `product_id` <type>,
  `number` <type>
);

CREATE TABLE `` (
  `product_id` <type>,
  `color_id` <type>,
  `size_id` <type>
);

CREATE TABLE `Color` (
  `id` <type>,
  `product_id` <type>,
  `color` <type>,
  `picture` <type>
);

CREATE TABLE `` (
  `product_id` <type>,
  `product_name` <type>,
  `product_brand` <type>,
  `product_price` <type>
);


